require('dotenv').config();const {Client,GatewayIntentBits,Collection}=require('discord.js');const {REST,Routes}=require('discord.js');const fs=require('fs'),path=require('path');const files=[];function walk(d){for(const x of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,x.name);x.isDirectory()?walk(p):x.name.endsWith('.js')&&files.push(p)}}walk(path.join(__dirname,'..','src','commands'));(async()=>{const body=files.map(f=>require(f).data.toJSON());const rest=new REST({version:'10'}).setToken(process.env.DISCORD_TOKEN);if(process.env.GUILD_ID)await rest.put(Routes.applicationGuildCommands(process.env.CLIENT_ID,process.env.GUILD_ID),{body});else await rest.put(Routes.applicationCommands(process.env.CLIENT_ID),{body});console.log(`Registered ${body.length} commands.`)})().catch(e=>{console.error(e);process.exit(1)});
