const {SlashCommandBuilder}=require('discord.js');const {getDb}=require('../../database');const {parseDuration}=require('../../utils/time');module.exports={data:new SlashCommandBuilder().setName('remind').setDescription('Set a reminder').addStringOption(o=>o.setName('time').setDescription('10m, 2h, 1d').setRequired(true)).addStringOption(o=>o.setName('message').setDescription('Reminder').setRequired(true)),async execute(i){const ms=parseDuration(i.options.getString('time'));if(!ms)return i.reply({content:'Use 10m, 2h, 1d, etc.',ephemeral:true});getDb().prepare('INSERT INTO reminders(guild_id,user_id,remind_at,message) VALUES(?,?,?,?)').run(i.guildId,i.user.id,Date.now()+ms,i.options.getString('message'));return i.reply('⏰ Reminder saved.');}};
