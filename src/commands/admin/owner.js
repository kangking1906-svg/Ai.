const {SlashCommandBuilder}=require('discord.js');const config=require('../../config');module.exports={data:new SlashCommandBuilder().setName('owner').setDescription('Owner-only controls').addSubcommand(s=>s.setName('stats').setDescription('Bot process stats')).addSubcommand(s=>s.setName('maintenance').setDescription('Show maintenance state').addBooleanOption(o=>o.setName('enabled').setDescription('Enabled').setRequired(true))),async execute(i,client){if(i.user.id!==config.ownerId)return i.reply({content:'❌ Owner only.',ephemeral:true});const p=i.options.getSubcommand();if(p==='stats')return i.reply(`Guilds: ${client.guilds.cache.size}\nUsers cached: ${client.users.cache.size}\nMemory: ${Math.round(process.memoryUsage().rss/1024/1024)}MB\nUptime: ${Math.floor(process.uptime())}s`);return i.reply(`Maintenance mode requested: ${i.options.getBoolean('enabled')}. Use your deployment provider to actually stop public interactions.`);}};
