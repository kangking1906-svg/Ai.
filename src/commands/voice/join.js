const {SlashCommandBuilder,PermissionFlagsBits}=require('discord.js');const v=require('../../services/voice');module.exports={data:new SlashCommandBuilder().setName('join').setDescription('Join your current voice channel'),async execute(i){const c=i.member.voice.channel;if(!c)return i.reply({content:'❌ Join a voice channel first.',ephemeral:true});if(!i.guild.members.me.permissionsIn(c).has([PermissionFlagsBits.Connect,PermissionFlagsBits.Speak]))return i.reply({content:'❌ I need Connect and Speak.',ephemeral:true});await v.join(c);return i.reply(`✅ Joined **${c.name}**.`);}};
