const {SlashCommandBuilder}=require('discord.js');const v=require('../../services/voice');module.exports={data:new SlashCommandBuilder().setName('speak').setDescription('Speak in voice').addStringOption(o=>o.setName('text').setDescription('Text').setRequired(true)).addStringOption(o=>o.setName('voice').setDescription('Voice name')),async execute(i){await i.deferReply();try{await v.speak(i.guildId,i.options.getString('text'),i.options.getString('voice')||undefined);await i.editReply('🔊 Done.');}catch(e){await i.editReply(`❌ ${e.message}`);}}};
